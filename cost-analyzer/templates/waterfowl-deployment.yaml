{{- if and (not .Values.agent) (not .Values.cloudAgent) (.Values.waterfowl) }}
{{- if gt (.Values.waterfowl.replicas | toString | atoi) 0 }}

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ template "waterfowl.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{ include "waterfowl.commonLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.waterfowl.replicas }}
  serviceName: "waterfowl"
  selector:
    matchLabels:
      app.kubernetes.io/name: waterfowl
      app.kubernetes.io/instance: {{ .Release.Name }}
      app: waterfowl
  volumeClaimTemplates:
  - metadata:
      name: persistent-configs
    spec:
      accessModes: [ "ReadWriteOnce" ]
      storageClassName: {{ .Values.waterfowl.persistentConfigsStorage.storageClass }}
      resources:
        requests:
          storage: {{ .Values.waterfowl.persistentConfigsStorage.storageRequest }}
  - metadata:
      name: waterfowl-storage
    spec:
      accessModes: [ "ReadWriteOnce" ]
      storageClassName: {{ .Values.waterfowl.waterfowlStorage.storageClass }}
      resources:
        requests:
          storage:  {{ .Values.waterfowl.waterfowlStorage.storageRequest }}
  {{- if .Values.waterfowl.waterfowlDbStorage }}
  - metadata:
      name: waterfowl-db-storage
    spec:
      accessModes: [ "ReadWriteOnce" ]
      storageClassName: {{ .Values.waterfowl.waterfowlDbStorage.storageClass }}
      resources:
        requests:
          storage:  {{ .Values.waterfowl.waterfowlDbStorage.storageRequest }}
  {{- end }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: waterfowl
        app.kubernetes.io/instance: {{ .Release.Name }}
        app: waterfowl
    spec:
      restartPolicy: Always
      serviceAccountName: {{ template "waterfowl.serviceAccountName" . }}
      volumes:
        {{- $etlBackupBucketSecret := "" }}
        {{- if .Values.kubecostModel.federatedStorageConfigSecret }}
            {{- $etlBackupBucketSecret = .Values.kubecostModel.federatedStorageConfigSecret }}
        {{- end }}
        {{- if $etlBackupBucketSecret }}
        - name: bucket-config
          secret:
           defaultMode: 420
           secretName: {{ $etlBackupBucketSecret }}
        {{- end }}
      initContainers:
        - name: config-db-perms-fix
          image: busybox
          command: ["sh", "-c", "/bin/chmod -R 777 /var/configs"]
          volumeMounts:
            - name: persistent-configs
              mountPath: /var/configs
            - name: waterfowl-storage
              mountPath: /var/configs/waterfowl
            {{- if .Values.waterfowl.waterfowlDbStorage }}
            - name: waterfowl-db-storage
              mountPath: /var/configs/waterfowl/duckdb
            {{- end }}
          securityContext:
            runAsUser: 0
      containers:
        - name: waterfowl
          {{- if .Values.kubecostModel }}
          {{- if .Values.kubecostModel.openSourceOnly }}
          {{- fail "Waterfowl cannot be used with open source only" }}
          {{- else if .Values.waterfowl.fullImageName }}
          image: {{ .Values.waterfowl.fullImageName }}
          {{- else if .Values.kubecostModel.fullImageName }}
          image: {{ .Values.kubecostModel.fullImageName }}
          {{- else if .Values.imageVersion }}
          image: {{ .Values.kubecostModel.image }}:{{ .Values.imageVersion }}
          {{- else }}
          image: {{ .Values.kubecostModel.image }}:prod-{{ $.Chart.AppVersion }}
          {{ end }}
          {{- else }}
          image: gcr.io/kubecost1/cost-model:prod-{{ $.Chart.AppVersion }}
          {{ end }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: 9004
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 200
          imagePullPolicy: Always
          args: ["waterfowl"]
          ports:
            - name: tcp-api
              containerPort: 9004
              protocol: TCP
          resources:
{{ toYaml .Values.waterfowl.resources | indent 12 }}
          volumeMounts:
            - name: persistent-configs
              mountPath: /var/configs
            - name: bucket-config
              mountPath: /var/configs/etl
            - name: waterfowl-storage
              mountPath: /var/configs/waterfowl
            {{- if .Values.waterfowl.waterfowlDbStorage }}
            - name: waterfowl-db-storage
              mountPath: /var/configs/waterfowl/duckdb
            {{- end }}
          env:
            {{- if and (.Values.prometheus.server.global.external_labels.cluster_id) (not .Values.prometheus.server.clusterIDConfigmap) }}
            - name: CLUSTER_ID
              value: {{ .Values.prometheus.server.global.external_labels.cluster_id }}
            {{- end }}
            {{- if .Values.prometheus.server.clusterIDConfigmap }}
            - name: CLUSTER_ID
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.prometheus.server.clusterIDConfigmap }}
                  key: CLUSTER_ID
            {{- end }}

            - name: CONFIG_PATH
              value: /var/configs/
            - name: ETL_ENABLED
              value: "false" # this pod should never run KC's concept of "ETL"
            - name: CLOUD_PROVIDER_API_KEY
              value: "AIzaSyDXQPG_MHUEy9neR7stolq6l0ujXmjJlvk" # The GCP Pricing API key.This GCP api key is expected to be here and is limited to accessing google's billing API.'
              value: "true" # just in case, not sure if necessary

            {{- if $etlBackupBucketSecret }}
            # If this isn't set, we pretty much have to be in a read only state,
            # initialization will probably fail otherwise.
            - name: ETL_BUCKET_CONFIG
              {{- if not .Values.kubecostModel.federatedStorageConfigSecret}}
              value: "/var/configs/etl/object-store.yaml"
              {{- else  }}
              value: "/var/configs/etl/federated-store.yaml"
            - name: FEDERATED_STORE_CONFIG
              value: "/var/configs/etl/federated-store.yaml"
            - name: FEDERATED_PRIMARY_CLUSTER # this ensures the ingester runs assuming federated primary paths in the bucket
              value: "true"
            - name: FEDERATED_CLUSTER # this ensures the ingester runs assuming federated primary paths in the bucket
              value: "true"
              {{- end }}
            {{- end }}

            # TODO (michaelmdresser):
            # - SAML configs?
            # - Product key configs?

            {{- range $key, $value := .Values.waterfowl.env }}
            - name: {{ $key | quote }}
              value: {{ $value | quote }}
            {{- end }}
            - name: KUBECOST_NAMESPACE
              value: {{ .Release.Namespace }}

    {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
      {{ toYaml .Values.imagePullSecrets | indent 2 }}
    {{- end }}
      {{- if .Values.waterfowl.priority }}
      {{- if .Values.waterfowl.priority.enabled }}
      {{- if .Values.waterfowl.priority.name }}
      priorityClassName: {{ .Values.waterfowl.priority.name }}
      {{- else }}
      priorityClassName: {{ template "cost-analyzer.fullname" . }}-waterfowl-priority
      {{- end }}
      {{- end }}
      {{- end }}
      {{- with .Values.waterfowl.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.waterfowl.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.waterfowl.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
{{- end }}
